#include "Graph.h"
#include <string>






   /* void Graph::addEdge(int s, int destination, double weight) {
        Edge* edge = nullptr;

        for (Edge* e : edges) {
            if (s == e->u) {
                edge = e;
                break;
            }
        }

        if (edge == nullptr) {
            edge = new Edge(s);
            edges.push_back(edge);
        }

        edge->addDestination(destination, weight);
    }
*/
